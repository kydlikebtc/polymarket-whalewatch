{
  "name": "whalewatch-x-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "把 whalewatch 的信号帖通过本机已登录的 X 会话发出去",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.6",
    "jsdom": "^30.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
