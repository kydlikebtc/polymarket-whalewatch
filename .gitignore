node_modules/
.env
.env.local
*.sqlite*
*.db
dist/
.next/
*.tsbuildinfo

# research scratch (one-off probing, findings live in docs/plans)
__pycache__/
*.pyc
*.py
probe_result.json
abi_*.json
sample_*.json

# session artifacts
.claude/
.playwright-mcp/
next-env.d.ts

# macOS Finder droppings. Deliberately unanchored (no leading slash) so it
# matches at every depth — Finder drops one of these into any directory that
# gets browsed, and docs/ is the one people actually open in Finder.
.DS_Store
